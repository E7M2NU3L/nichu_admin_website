import { createSlice } from "@reduxjs/toolkit";

const initialState = [

];

const DashSlice = createSlice({
    name: "dashboard",
    initialState,
    reducers: {
        change: {

        }
    }
})

export default DashSlice.reducer;
